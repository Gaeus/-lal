<div class="container-local">
  <h2> SHOW PAGE </h2>
          <img src="<%=  @hideout.user.picture %>" alt="avatar" class ="avatar-large">
        <p>Pour plus d'informations, vous pouvez contactez <%=@hideout.user.nickname %> : <%=@hideout.user.email %></p>
  <div class="show-hideout">
    <div>
      <img src="<%=  @hideout.cover_picture %>" alt="Cover Image">
    </div>
    <div class=show-hideout-content>
      <div>
        <h2><%= @hideout.title %></h2>
        <p><%= @hideout.address %></p>
        <p><%= @hideout.overview %></p>
      </div>
      <div>
        <p class="text-right"><%= @hideout.price %> €</p>
      </div>
    </div>
  </div>
</div>


<% if current_user != @hideout.user %>

  <div data-controller="toggle" class="mt-5">
      <button data-action="click->toggle#fire" class="btn btn-outline-primary">Reserver</button>
    <div data-toggle-target="togglableElement" class="d-none">
      <%= render "shared/form_reservation", booking: @booking, hideout: @hideout %>
    </div>
  </div>
<% end %>
<%= console %>
