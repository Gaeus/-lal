<!DOCTYPE html>
<html>
  <head>
    <title>Lairs & Lodges</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Hind+Vadodara:wght@300;400;500;600;700&family=Homenaje&family=Oxygen:wght@300;400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="wrapper">
      <%= render "shared/navbar" %>
      <p style="height: 0; padding:0; margin: 0;" class="notice"><%= notice %></p>
      <p style="height: 0; padding:0; margin: 0;" class="alert"><%= alert %></p>
      <main class="content">
        <%= yield %>
      </main>
        <%= render "shared/footer" %>
    </div>
  </body>
</html>
